package com.martyworm.entites.minion;

import org.junit.Test;

public class MinionTests {

    @Test
    public void shouldMoveToAValidTileNextToIt(){

        //because movement is based on a tile rather than x/y position

        //first we must set up a simple map w/ tiles (i suggest 2 tiles)

        //create a minion

        //put the minion on the left tile

        //send the move command

        //validate the minion is on the correct tile
    }
}
